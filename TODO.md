# TODO (in order)

1) Add Planner page that reads `state/plan.json` and renders:
   - Today card
   - Week card
   - Calendar (react-big-calendar)
2) Add Vercel deploy GitHub Action (PR preview + main deploy).
3) Add state validation in CI (schema check).
